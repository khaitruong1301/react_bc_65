

export const ButtonPrimary = () => { //Tạo component sẽ dùng function hoặc arrow function

    //Nội dung của component phải viết trong lệnh return (giao diện của thẻ component)
    return <> 
        <button style={{ background: 'red' }}>
            <span>Button primary</span>
        </button>

    </>
}

